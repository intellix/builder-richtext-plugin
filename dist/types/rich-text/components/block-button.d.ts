import React from "react";
import { CustomElementFormat } from "../slate.types";
interface BlockButtonProps {
    format: CustomElementFormat;
    icon: string;
}
export declare const BlockButton: ({ format, icon }: BlockButtonProps) => React.JSX.Element;
export {};
